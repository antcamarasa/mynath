<%= simple_form_for([@appointment, @prescription]) do |f| %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="container mt-5 background text-center">
    <div>
      <h4>Votre ordonnance</h4>
    </div>
    <div class="form-inputs">
      <%= f.input :description, label: 'Ajouter une description'  %>
      <%= f.input :document, as: :file, label: 'Télécharger votre document'  %>
    </div>
    <div>
      <div>
      <%= link_to 'Back', prescriptions_path, class:"button-vert" %>
      </div>
      <div class="form-actions">
        <%= f.button :submit, class: "button-vert" %>
      </div>
    </div>
  </div>
 <% end %>
